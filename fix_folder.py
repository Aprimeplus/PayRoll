import os
import shutil

# รายชื่อไฟล์ที่ต้องย้ายเข้า resources
files_to_move = [
    "THSarabunNew.ttf",
    "THSarabunNew Bold.ttf",
    "THSarabunNew Italic.ttf", 
    "THSarabunNew BoldItalic.ttf"
]

# สร้างโฟลเดอร์ resources ถ้ายังไม่มี
if not os.path.exists("resources"):
    os.makedirs("resources")
    print("✅ สร้างโฟลเดอร์ 'resources' เรียบร้อยแล้ว")
else:
    print("ℹ️ โฟลเดอร์ 'resources' มีอยู่แล้ว")

# ย้ายไฟล์
count = 0
for file in files_to_move:
    # เช็คว่ามีไฟล์อยู่ข้างนอกไหม
    if os.path.exists(file):
        # ย้ายเข้าไปข้างใน
        try:
            shutil.move(file, os.path.join("resources", file))
            print(f"✅ ย้ายไฟล์ {file} -> เข้าไปใน resources สำเร็จ")
            count += 1
        except Exception as e:
            print(f"❌ ย้ายไฟล์ {file} ไม่สำเร็จ: {e}")
    # เช็คว่าไฟล์อยู่ใน resources หรือยัง
    elif os.path.exists(os.path.join("resources", file)):
        print(f"👍 ไฟล์ {file} อยู่ใน resources เรียบร้อยแล้ว")
    else:
        print(f"⚠️ ไม่พบไฟล์ {file} ทั้งข้างนอกและข้างใน (กรุณาหาไฟล์มาใส่)")

print(f"\nสรุป: ย้ายไฟล์สำเร็จ {count} ไฟล์")
print("กรุณารัน PyInstaller ใหม่อีกครั้ง")